const LOADING = '/';
const WELCOME = '/welcome';
const ONBOARDING = '/onboarding';
const WALLET = '/wallet';

const ROUTES = {
  LOADING,
  WELCOME,
  ONBOARDING,
  WALLET,
};

export default ROUTES;
